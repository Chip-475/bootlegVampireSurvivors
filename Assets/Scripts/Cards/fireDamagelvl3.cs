using UnityEngine;

public class fireDamagelvl3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
         //VEDERE LA CLASSE SCYTHE
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
